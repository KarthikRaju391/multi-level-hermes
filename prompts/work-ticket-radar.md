Run the `work-ticket-radar` skill.

Use only configured read-only Linear/ClickUp access. Summarize assigned, mentioned, due-soon, blocked, urgent, and recently changed tickets into Markdown under `memory/work/**`. Do not create, comment, assign, close, prioritize, or edit tickets. Store source pointers instead of raw ticket histories.

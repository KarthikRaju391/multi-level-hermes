Run the `work-email-radar` skill.

Use only configured read-only work email access. Summarize important changes since the last run into Markdown under `memory/work/**`. Prioritize direct requests, deadlines, blockers, escalations, incidents, client/customer impact, and ticket/project mentions. Do not read personal email. Do not take any external action. Store source pointers instead of raw full emails.

Run the `personal-email-radar` skill.

Use only configured read-only personal email access. Summarize important changes since the last run into Markdown under `memory/personal/**`. Do not read work email. Do not take any external action. Store source pointers instead of raw full emails.

# Work cron

Create cron jobs with `scripts/create-cron-jobs.sh` after configuring read-only work email and ticket integrations.

Do not hand-edit Hermes `jobs.json` unless you know your installed Hermes version's exact schema.

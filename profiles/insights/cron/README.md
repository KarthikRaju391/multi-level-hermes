# Insights cron

Create the daily review cron job with `scripts/create-cron-jobs.sh` after personal/work radar jobs are producing Markdown summaries.

Do not hand-edit Hermes `jobs.json` unless you know your installed Hermes version's exact schema.

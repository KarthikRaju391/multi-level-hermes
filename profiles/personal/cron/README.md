# Personal cron

Create cron jobs with `scripts/create-cron-jobs.sh` after configuring a read-only personal email integration.

Do not hand-edit Hermes `jobs.json` unless you know your installed Hermes version's exact schema.
